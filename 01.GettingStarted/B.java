import java.util.Scanner;

public class B {
    public int cube(int value) {
        int num = value;
        double pow = Math.pow(num, 3);
        int answer = (int)pow;
        return answer;
    }
}
